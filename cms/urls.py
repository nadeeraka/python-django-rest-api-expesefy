# from django.urls import  path , include
# from app import views
# from rest_framework import routers
#
# router = routers.DefaultRouter()
# router.register(r'todos', views.TodoViewSet)
# router.register(r'notesCollection', views.NoteCollectionViewSet)
# router.register(r'todoCollection', views.TodoCollectionViewSet)
# router.register(r'goal', views.GoalViewSet)
# router.register(r'note', views.NoteViewSet)
#
#
#
# urlpatterns = [
#        path('', include(router.urls)),
# ]

#
# urlpatterns = [
#     path('current_user/', views.current_user),
#     path('userss/', views.UserList.as_view()),
#     path('userProfile/', views.UserProfile.as_view()),
#     path('delayQuantity/', views.DelayQuantity.as_view()),
#     path('plantUsage/', views.PlantUsage.as_view()),
#     path('taktTime/', views.TaktTimeView.as_view()),
#     path('testView/', views.TestView.as_view()),
#     path('', include(router.urls)),
#     re_path(r'ws/test/$', consumers.TestConsumer),
# ]
